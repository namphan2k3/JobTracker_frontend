# API base URL cho authenticated requests (qua proxy khi dev)
VITE_API_BASE=/api/v1

# API URL gọi thẳng backend (không qua proxy) — dùng cho public endpoints, download...
# Dev: http://localhost:8080/api/v1
# Prod: https://jobtracker-ats-production.up.railway.app/api/v1
VITE_API_DIRECT=http://localhost:8080/api/v1
